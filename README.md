# server-torosalmonlover

## 1回目の授業
```bash
# SSH key を作成
$ ssh-keygen -t ed25519
$ cat ~/.ssh/id_ed25519.pub
# リポジトリをファイルにクローン
$ git clone git@github.com:kdg-server-2025/server-torosalmonlover.git
```